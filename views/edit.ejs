<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Note</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-r from-gray-900 to-gray-800 text-white min-h-screen flex items-center justify-center">

  <!-- Main Container -->
  <div class="w-full max-w-lg p-8 bg-gray-800 rounded-xl shadow-lg border border-gray-700">

    <!-- Form Header -->
    <h2 class="text-3xl font-semibold text-center text-blue-400 mb-6">Update Note</h2>

    <!-- Update Note Form -->
    <form action="/edit" method="POST" class="space-y-6">

      <!-- Previous Name Input -->
      <div>
        <label for="previousName" class="block text-sm font-medium text-gray-200">Current Note Heading</label>
        <input 
          required 
          type="text" 
          name="previousName" 
          value="<%= title %>" 
          class="w-full py-3 px-4 mt-2 bg-gray-700 text-gray-200 rounded-md shadow-md placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500 cursor-not-allowed"
          placeholder="Current Note Heading"
          readonly
        />
      </div>

      <!-- New Name Input -->
      <div>
        <label for="newName" class="block text-sm font-medium text-gray-200">New Note Heading</label>
        <input 
          required 
          type="text" 
          name="newName" 
          class="w-full py-3 px-4 mt-2 bg-gray-700 text-gray-200 rounded-md shadow-md placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
          placeholder="Enter Your New Note Heading"
        />
      </div>

      <!-- Submit Button -->
      <div>
        <button 
          type="submit" 
          class="w-full py-3 bg-blue-600 hover:bg-blue-500 transition duration-200 rounded-md shadow-md text-white font-medium focus:outline-none focus:ring-2 focus:ring-blue-500"
        >
          Update File Name
        </button>
      </div>

    </form>

  </div>

</body>

</html>
